# Screenshots

Drop the four Biblio code-review PNGs here, named exactly:

- `review-1-facets.png` — the per-facet review comment (LOGICAL / XUNIT TESTS / TECHNIQUE / PERFORMANCE / WONDERLY APP / STRUCTURAL)
- `review-2-finding.png` — an expanded finding with file/line and the proposed fix
- `review-3-summary-tools.png` — the severity summary table + "Tool Usage" (SearchSections / RecallMemory / SearchCodeSamples)
- `review-4-sources.png` — the "Sources Used" table (document/memory, partition, facets, use)

Optional cropping tip: the four screenshots share the same GitHub PR header bar. Cropping the top ~90px of chrome off images 2–4 makes the stepped gallery read more cleanly, but it's not required — the component frames them either way.
